require('dotenv').config();const bcrypt=require('bcryptjs');const sqlite3=require('sqlite3').verbose();const path=require('path');const email=(process.argv[2]||process.env.ADMIN_EMAIL||'').toLowerCase().trim();const password=process.argv[3]||process.env.ADMIN_PASSWORD;if(!email||!password){console.error('Uso: node scripts/create-admin.js email senha');process.exit(1)}const db=new sqlite3.Database(path.resolve(__dirname,'..',process.env.DB_PATH||'./data/atelier.db'));const hash=bcrypt.hashSync(password,12);db.serialize(()=>{db.run('CREATE TABLE IF NOT EXISTS users(id INTEGER PRIMARY KEY AUTOINCREMENT,email TEXT UNIQUE NOT NULL,password_hash TEXT NOT NULL,role TEXT NOT NULL DEFAULT \'admin\',created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)');db.run('INSERT INTO users(email,password_hash,role) VALUES(?,?,\'admin\') ON CONFLICT(email) DO UPDATE SET password_hash=excluded.password_hash,role=\'admin\'',[email,hash],e=>{if(e){console.error(e);process.exitCode=1}else console.log('Administrador configurado:',email);db.close()})});
