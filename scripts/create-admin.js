require('dotenv').config();const bcrypt=require('bcryptjs');const sqlite3=require('sqlite3').verbose();const path=require('path');
const db=new sqlite3.Database(path.resolve(__dirname,'..',process.env.DB_PATH||'./data/atelier.db'));const email=process.argv[2]||process.env.ADMIN_EMAIL;const password=process.argv[3]||process.env.ADMIN_PASSWORD;if(!email||!password){console.error('Uso: node scripts/create-admin.js email senha');process.exit(1)}const hash=bcrypt.hashSync(password,12);db.run('CREATE TABLE IF NOT EXISTS users(id INTEGER PRIMARY KEY AUTOINCREMENT,email TEXT UNIQUE NOT NULL,password_hash TEXT NOT NULL,role TEXT NOT NULL DEFAULT \'admin\',created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)',()=>db.run('INSERT OR REPLACE INTO users(email,password_hash,role) VALUES(?,?,?)',[email,hash,'admin'],e=>{if(e)console.error(e);else console.log('Administrador gravado:',email);db.close()}));
