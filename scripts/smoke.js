const http=require('http');const port=process.env.PORT||3000;const checks=['/api/health','/api/settings','/api/categories','/api/collections','/api/dresses'];let pending=checks.length,fail=0;for(const p of checks){http.get(`http://127.0.0.1:${port}${p}`,r=>{let d='';r.on('data',c=>d+=c);r.on('end',()=>{console.log(r.statusCode,p);if(r.statusCode!==200)fail++;if(!--pending)process.exit(fail?1:0)})}).on('error',e=>{console.error('FAIL',p,e.message);if(!--pending)process.exit(1)})}
